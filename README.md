# variant_concordance_pipeline
Evaluates variant concordance between two VCF files (baseline and query) using RTG Tools vcfeval. Annotates discordant variants by failure mode and generates an interactive HTML report.
